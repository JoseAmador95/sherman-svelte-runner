# Artefactos de bootstrap (se quedan en local, no forman parte del repo).
*.tar.gz
shermansvelterunnerkickoff.md

# Cruft del SO.
.DS_Store

# Configuración de los avisos del vigía: contiene credenciales. La escribe
# configurar-avisos.sh en ./vigia/ del despliegue; si ese despliegue es este
# mismo clon, que no acabe en un commit.
avisos.conf

# Artefactos de un despliegue hecho DENTRO de este clon. Lo recomendado es un
# directorio dedicado, pero se hace, y `.env` lleva el PAT: que un `git add -A`
# no lo pueda subir.
.env
access_token
compose.yaml
vigia/
